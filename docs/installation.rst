.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install arwn

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv arwn
    $ pip install arwn
